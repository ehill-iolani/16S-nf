# Nextflow
work/
.nextflow/
.nextflow.log*
results/

# raw sequencing data and reference databases -- these are large,
# run-specific, and not part of the pipeline itself; point --input /
# --taxdb / --metadata at your own copies. A small synthetic dataset for
# -profile test lives under tests/data/ instead, and is tracked normally.
/data/
assets/*.fasta
assets/*.csv
assets/tax_db_cache/

# personal/local
scratch.sh
notes.md
notes.sh

# OS
.DS_Store
**/.DS_Store
